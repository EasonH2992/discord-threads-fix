
services:
  threads-bot:
    build: .
    container_name: threads-preview-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
    # Alternatively, you can define the token here directly for Unraid template
    # environment:
    #   - DISCORD_TOKEN=your_token_here
